package com.inherit.implemnts;

public interface USgovt {
	
	int max_tax = 50000;
	
	public void savings();
	public void incometax();

	//only method declaration, no method body
	//we can declare variable that is static by default
	//we can't change var values
	//no main methods and static methods in interface
	//can't create the object of interface means abstract in nature
}
